services:
  backend:
    build: ./backend
    ports:
      - "8000:8000"
    # Uncomment to enable the optional LLM step (see backend/.env.example).
    # The app runs fine without it — it just skips the HyDE query rewrite.
    # env_file:
    #   - ./backend/.env
